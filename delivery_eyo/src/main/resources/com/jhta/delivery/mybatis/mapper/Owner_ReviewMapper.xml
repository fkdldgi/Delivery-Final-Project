<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.Owner_ReviewMapper">

	<!-- 가게에 대한 리뷰 리스트 -->
	<select id="ReviewList" parameterType="int" resultType="com.jhta.delivery.vo.Owner_ReviewVo">
		select * from review where shop_num= order by regdate
	</select>
</mapper>